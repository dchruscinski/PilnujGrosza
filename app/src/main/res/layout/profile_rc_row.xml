<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

    <ImageView
        android:id="@+id/prof_rc_icon"
        android:layout_width="50dp"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginHorizontal="10dp"
        android:src="@drawable/baseline_account_circle_24" />

    <TextView
        android:id="@+id/prof_rc_secondLine"
        android:layout_width="fill_parent"
        android:layout_height="26dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@id/prof_rc_icon"
        android:gravity="center_vertical"
        android:ellipsize="marquee"
        android:text="@string/prof_rc_secondLine"
        android:textSize="12sp"
        android:singleLine="true" />

    <TextView
        android:id="@+id/prof_rc_firstLine"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/prof_rc_secondLine"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toEndOf="@id/prof_rc_icon"
        android:gravity="center_vertical"
        android:text="@string/prof_rc_firstLine"
        android:textSize="16sp" />

    <ImageView
        android:id="@+id/prof_rc_editicon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_edit_black_24dp"/>

</RelativeLayout>